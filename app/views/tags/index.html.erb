<h1 class="my-3">Tagy</h1>

<%= render 'flash_messages' %>
<%= render partial: 'breadcrumb', locals: {
  breadcrumb: {
    'Nastavení' => settings_path,
    'Tagy' => nil,
  }
} %>

<aside class="my-2"><%= link_to 'Přidat nový tag', new_tag_path %></aside>

<%= render partial: 'list', locals: { tags: @tags } %>
