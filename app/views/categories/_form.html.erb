<%= simple_form_for(@category) do |f| %>
  <%= f.input :title, label: 'Název' %>
  <%= f.input :color, input_html: { type: "color" }, label: 'Barva' %>
  <%= f.button :submit, 'Uložit kategorii', class: 'btn-primary' %>
<% end %>
